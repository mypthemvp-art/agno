You are building StrategyIQ, Bloomberg Terminal for retail. Next.js 14 + FastAPI + TimescaleDB + Pinecone + Redis + Stripe. Tiers: beginner (3/day delayed), pro ($29 real-time), elite ($79 PORT+EQS). Always add SEC disclaimer. Never scrape, use Polygon/CoinGecko/FMP licensed APIs. Tier gating via Depends(get_user_tier). EQS via routers/screener.py 50 filters, PORT via PortGo.tsx VaR/Sharpe/Monte Carlo, Grok via integrations/grok.py. DB via Alembic. Edge: /api/chat uses Upstash ratelimit for beginner tier.
